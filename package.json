{
  "name": "merge-conflict-radar",
  "version": "1.0.0",
  "description": "GitHub Action that warns when open pull requests touch the same files, so you can coordinate merge order before a conflict happens.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist --license licenses.txt",
    "test:syntax": "node -c src/index.js"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "minimatch": "^9.0.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
